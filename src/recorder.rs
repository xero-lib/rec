pub mod audio;
mod file_io;

// struct NotRecording;
// struct Recording;

// pub trait Recorder<State = Recording>
// where Self: Sized {
//     fn record(&mut self) -> Result<Self::<State>, Box<dyn std::error::Error>>;
//     fn stop_recording(&mut self)
// }
